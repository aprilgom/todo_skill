# todo CLI

The canonical usage guide lives at [../../README.md](../../README.md).

This directory contains the Go implementation:

```sh
go test ./...
go build -o ../todo ./cmd/todo
../todo --help
```
